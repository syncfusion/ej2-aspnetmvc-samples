@using Syncfusion.EJ2;
@section Meta{
                    <meta name="description" content="This example demonstrates the Empty Points in ASP.NET MVC HeatMap Chart control. Explore here for more details."/>
                }
@section ControlsSection{
    <div class="control-section">
        <div id="container">
            @Html.EJS().HeatMap("container").TitleSettings(titleSettings => titleSettings.Text("Defective Count per 1000 Products from a Manufacturing Unit").TextStyle(ViewData["textStyle"])).XAxis(
            xAxis =>
            {
                xAxis.Labels((string[])ViewData["xLabels"]).TextStyle(ViewData["labelTextStyle"]);
            }).YAxis(yaxis =>
            {
                yaxis.Labels((string[])ViewData["yLabels"]).TextStyle(ViewData["labelTextStyle"]);
            }).LegendSettings(legendSettings => legendSettings.Position(Syncfusion.EJ2.HeatMap.LegendPosition.Bottom).Width("250px").ShowLabel(true).TextStyle(ViewData["labelTextStyle"])).PaletteSettings(paletteSettings => paletteSettings.Palette(palette =>
            {
                palette.Color("rgb(172, 213, 242)").Add();
                palette.Color("rgb(127, 168, 201)").Add();
                palette.Color("rgb(82, 123, 160)").Add();
                palette.Color("rgb(37, 78, 119)").Add();
            }).Type(Syncfusion.EJ2.HeatMap.PaletteType.Gradient)).CellSettings(cellSettings =>
            cellSettings.Border(ViewData["border"]).ShowLabel(true).TextStyle(ViewData["labelTextStyle"])).TooltipSettings(tooltipSettings => tooltipSettings.TextStyle(textStyle => textStyle.FontFamily("inherit"))).TooltipRender("tooltipRender").Load("load").DataSource(ViewData["dataSource"]).Render()
        </div>
    </div>

    <style>
        #control-container {
            padding: 0px !important;
        }
    </style>
    <script>
        var tooltipRender = function (args) {
            args.content = [args.yLabel + ' | ' + args.xLabel + ' : ' + args.value + ' defective units'];
        };
        var load = function (args) {
            @* custom code start *@
            var selectedTheme = themeName ? themeName : 'Material';
            args.heatmap.theme = (selectedTheme.charAt(0).toUpperCase() + selectedTheme.slice(1)).replace(/-dark/i, 'Dark').replace(/-high/i, 'High').replace(/contrast/i, 'Contrast').replace(/5.3/i, '5');
            @* custom code end *@
        };
        var dataSource = window.emptyPointDataSource;

    </script>
}
@section ActionDescription{
    <p>
        This sample visualizes the number of defective product count per 1000 units coming out from a manufacturing unit.
        Data points are enhanced with labels and tooltip. Some data points were not marked with any values which indicates there are no defective products and these data points are termed as empty points.
    </p>
}
@section Description{
    <p>
        In this example, you can see how to render empty points in the Heatmap. The empty points or the points with no data
        can be marked using <code>null</code> in the data source. You can also customize the background color of the
        empty points by using the <a href="https://help.syncfusion.com/cr/aspnetmvc-js2/Syncfusion.EJ2.HeatMap.HeatMapPaletteSettings.html#Syncfusion_EJ2_HeatMap_HeatMapPaletteSettings_EmptyPointColor" target='_blank'>emptyPointColor<span class="sr-only">opens in a new tab</span></a> property in <a href="https://help.syncfusion.com/cr/aspnetmvc-js2/Syncfusion.EJ2.HeatMap.HeatMapPaletteSettings.html" target='_blank'>paletteSettings<span class="sr-only">opens in a new tab</span></a>.
    </p>
    <p>The tooltip is enabled in this example. To see the tooltip in action, hover the mouse over an item or tap an item on touch-enabled devices.</p>
    <br>
}
@section Title{
                    <title>ASP.NET MVC HeatMap Chart Empty Points Example - Syncfusion Demos </title> 
                }
@section Header{
                    <h1 class='sb-sample-text'>Example of Empty Points in ASP.NET MVC HeatMap Chart Control</h1>
                }