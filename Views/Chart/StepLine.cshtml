@using Syncfusion.EJ2;
@section ControlsSection{
    <div class="control-section" align="center">

        @Html.EJS().Chart("container-vertical").Series(series =>
       {

           series.Type(Syncfusion.EJ2.Charts.ChartSeriesType.StepLine).XName("Period").YName("AUS_UnemploymentRate").Opacity(1)
           .Marker(mr => mr.Visible(true).Width(7).Height(7)).DataSource(ViewBag.StepLineData).Width(5).Add();
       }).PrimaryXAxis(px => px.ValueType(Syncfusion.EJ2.Charts.ValueType.DateTime)
       .MajorGridLines(mg=>mg.Width(0)).Minimum(new DateTime(1971, 01, 1)).Maximum(new DateTime(2012, 01, 1))
       .EdgeLabelPlacement(Syncfusion.EJ2.Charts.EdgeLabelPlacement.Shift)
       ).PrimaryYAxis(py => py.LabelFormat("{value}%").MajorTickLines(mt=>mt.Width(0)).Interval(10).LineStyle(ls=>ls.Width(0)).Title("Production (In Percentage)")
       ).Title("Fruit Production Statistics").ChartArea(area => area.Border(br => br.Width(0))).Tooltip(tt => tt.Enable(true).Shared(true).Header("<b>Fruit Production</b>").Format("${point.x} : <b>${point.y}</b>")).Load("load").Render()
        </div>
    
   @*custom code start*@
    <style>
        #control-container {
            padding: 0px !important;
        }
    </style>
    <script>
        var load = function (args) {
            var selectedTheme = themeName ? themeName : 'Material';
            args.chart.theme = (selectedTheme.charAt(0).toUpperCase() + selectedTheme.slice(1)).replace(/-dark/i, "Dark").replace(/contrast/i, 'Contrast');
            args.chart.width = ej.base.Browser.isDevice ? "100%" : "75%";
        }
    </script>
   @*custom code end*@
}
   @*custom code start*@
@section Meta{
                    <meta name="description" content="This example demonstrates the Step Line in ASP.NET MVC Chart control. Explore here for more details."/>
                }
   @*custom code end*@
@section ActionDescription{
    <p>
        This <a target="_blank" href="https://www.syncfusion.com/aspnet-mvc-ui-controls/charts/chart-types/stepline-chart">ASP.NET MVC Step Line Chart</a> example visualizes the fruit production statistics with default step line series in the chart.
    </p>
}
@section Description{
    <p>
        In this example, you can see how to render and configure a step line chart. This chart forms the step line progress by connecting points using vertical and horizontal lines. <code>Markers</code> are used to represent individual data and its values.
    </p>
    <p>
        <code>Tooltip</code> is enabled in this example. To see the tooltip in action, hover a point or tap on a point in touch enabled devices.
    </p>
    <p>
        More information about the step line series can be found in this
        <a target="_blank" href="https://ej2.syncfusion.com/aspnetmvc/documentation/chart/chart-types#line-charts">documentation section</a>.
    </p> 
}
@section Title{
                    <title>ASP.NET MVC Chart Step Line Example - Syncfusion Demos </title> 
                }
@section Header{
                    <h1 class='sb-sample-text'>Charts</h1>
                }