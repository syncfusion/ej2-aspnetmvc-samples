@using Syncfusion.EJ2;
@section ControlsSection{
<div class="control-section">
    <div style="text-align:center">



        @Html.EJS().Chart("container").Series(series =>
   {

       series.Type(Syncfusion.EJ2.Charts.ChartSeriesType.StackingColumn).XName("Period").YName("PVT_Consumption").DataSource(ViewBag.ChartPoints).Name("Private Consumption").Add();
       series.Type(Syncfusion.EJ2.Charts.ChartSeriesType.StackingColumn).XName("Period").YName("GOVT_Consumption").DataSource(ViewBag.ChartPoints).Name("Government Consumption").Add();
       series.Type(Syncfusion.EJ2.Charts.ChartSeriesType.StackingColumn).XName("Period").YName("Investment").DataSource(ViewBag.ChartPoints).Name("Investment").Add();
       series.Type(Syncfusion.EJ2.Charts.ChartSeriesType.StackingColumn).XName("Period").YName("Trade").DataSource(ViewBag.ChartPoints).Name("Net Foreign Trade").Add();
       series.Type(Syncfusion.EJ2.Charts.ChartSeriesType.Line).XName("Period").YName("GDP").Opacity(0.6).Marker(mr => mr.Visible(true).Height(7).Width(7)).DataSource(ViewBag.ChartPoints).Name("GDP").Width(2).Add();
   }).PrimaryYAxis(px => px.Minimum(-3).Maximum(3).Interval(1).Title("Growth (in Billion)").LabelFormat("{value}B").LineStyle(ls=>ls.Width(0)).MajorTickLines(mt=>mt.Width(0))
       ).PrimaryXAxis(px => px.Title("Years").MajorGridLines(mg=>mg.Width(0)).MajorTickLines(mt=>mt.Width(0)
       ).LabelIntersectAction(Syncfusion.EJ2.Charts.LabelIntersectAction.Rotate45).ValueType(Syncfusion.EJ2.Charts.ValueType.Category)
       ).Tooltip(tt => tt.Enable(true)).ChartArea(area => area.Border(br => br.Width(0))).LegendSettings(lg => lg.EnableHighlight(true)).Title("Annual Growth of GDP in France").Load("load").Render()

       
    </div>
</div>

 @*custom code start*@
<style>
    #control-container {
        padding: 0px !important;
    }
</style>
@*custom code end*@
    <script>
        var load = function (args) {
            var selectedTheme = themeName ? themeName : 'Material';
            args.chart.theme = (selectedTheme.charAt(0).toUpperCase() + selectedTheme.slice(1)).replace(/-dark/i, "Dark").replace(/contrast/i, 'Contrast');
            args.chart.width = ej.base.Browser.isDevice ? "100%" : "75%";
            args.chart.primaryXAxis.interval = ej.base.Browser.isDevice ? 2 : 1;
        };
    </script>     
}
 @*custom code start*@
@section Meta{
                    <meta name="description" content="This example demonstrates the Multi-Series Chart in ASP.NET MVC Chart control. Explore here for more details."/>
                }
 @*custom code end*@
@section ActionDescription{
    <p>
        This sample shows the annual growth of Gross Domestic Product (GDP) in France in a combination of line and stacked column series. A tooltip shows information about the data points.
    </p>
}
@section Description{
    <p>
        In this example, you can see how to render and configure different type of charts. You can chart multiple variables on the y-axis.
    </p>
    <p>
        <code>Tooltip</code> is enabled in this example. To see the tooltip in action, hover a point or tap on a point in touch enabled devices.
    </p>
    <p>
        More information about the multiple series can be found in this
        <a target="_blank" href="https://ej2.syncfusion.com/aspnetmvc/documentation/chart/chart-series#multiple-series">documentation section</a>.
    </p>
}
@section Title{
                    <title>ASP.NET MVC Chart Multi-Series Chart Example - Syncfusion Demos </title> 
                }
@section Header{
                    <h1 class='sb-sample-text'>Example of Multi-Series Chart in ASP.NET MVC Chart Control</h1>
                }