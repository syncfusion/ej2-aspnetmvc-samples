@using Syncfusion.EJ2
@model EJ2MVCSampleBrowser.Controllers.ColorPicker.ColorPickerModel
@section ControlsSection{
<div class="control-section">
    <div id="default-control">
        <h4>Choose a color</h4>
        @using (Html.BeginForm()) {
            @Html.EJS().ColorPickerFor(model => model.colorValue).Render()
            @Html.ValidationMessageFor(model => model.colorValue)
            @Html.EJS().Button("submit-button").Content("Submit").Render()
        }
    </div>
</div>
}

@section Meta{
<meta name="description" content="This example demonstates strongly typed color picker for control functionality with model binding of Syncfusion ASP.NET MVC Colorpicker control." />
}
@section ActionDescription{
<div id="action-description">
    <p>Click the submit button to post the current value of the ColorPicker. When submitting with color value `#000080`, validation error message will be shown by using Data Annotation.</p>
</div>
}
@section Description{
<div id="description">
    <p>
        This sample demonstrates strongly typed support in ColorPicker. You can bind model property to the ColorPicker by using Lambda Expression.
        While submitting form, the model property will be based on current <code>value</code> of the ColorPicker.
    </p>
    <p>
        More information about ColorPicker can be found in this
        <a target="_blank"
           href="https://ej2.syncfusion.com/aspnetmvc/documentation/color-picker/getting-started" aria-label="documentation section">
            documentation section
        <span class="sr-only">opens in a new tab</span></a>.
    </p>
</div>
}
@*custom code start*@
<style>
    #default-control {
        margin: 30px auto;
        padding-top: 50px;
        text-align: center;
    }

    #default-control .row {
        width: 100%;
    }

    #default-control h4 {
        margin-bottom: 20px;
    }

    .material-dark #default-control h4 {
        color: #fff;
    }

    .fabric-dark #default-control h4 {
        color: #fff;
    }

    .bootstrap-dark #default-control h4 {
        color: #fff;
    }

    .bootstrap5-dark #default-control h4 {
        color: #fff;
    }

    .tailwind-dark #default-control h4 {
        color: #fff;
    }

    .e-bigger #default-control h4 {
        font-size: 20px;
    }

    #submit-button {
        margin-top: 10px;
    }

    .field-validation-error,
    .field-validation-valid {
        color: red;
        display: block;
        margin: 10px 0;
    }
</style>
@*custom code end*@
