@using Syncfusion.EJ2;

@section ActionDescription{
    This sample demonstrates how to customize rows and columns in a spreadsheet using XlsIO.
}
@section Meta{
    <meta name="description" content="This sample demonstrates how to create Excel file with various row and column operations in ASP.NET MVC using Syncfusion XlsIO (Excel) library." />
}
@section Description{
    Rows and columns in Excel can be customized, as required, with a number of options. Some of the major manipulations that can be done with rows and columns are setting row height, setting column width, hiding or un-hiding, and grouping or ungrouping.
    <br />
    <br /><b>Features:</b>
    <br />
    <br />
    <ul>
        <li>Reading template document</li>
        <li>Setting row height</li>
        <li>Setting column width</li>
        <li>Hiding and un-hiding</li>
        <li>Grouping and ungrouping</li>
    </ul>
    <br />
}
@section ControlsSection{
    <div class="control-section">
        <h5>
            Click the button to view an Excel spreadsheet generated by Essential XlsIO. Please
            note that Microsoft Excel Viewer or Microsoft Excel is required to view the resultant document.
        </h5>
        <br />
        <h5>  Save As :</h5>
        <div class="celldiv">
            <table>
                <tr>
                    <td>
                        @Html.EJS().RadioButton("radio1").Label("XLS").Name("select").Value("xls").Checked(false).Render()
                    </td>
                    <td>
                        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                    </td>
                    <td>
                        @Html.EJS().RadioButton("radio2").Label("XLSX").Name("select").Value("xlsx").Checked(true).Render()
                    </td>
                </tr>
            </table>
        </div>
        <br />
        <div class="celldiv">
            @Html.EJS().Button("createbtn").Content("Create Document").Render()
            @Html.EJS().Button("inputbtn").Content("Input Template").Render()
        </div>
    </div>
    <script>
        document.getElementById('createbtn').onclick = function () {
        if (document.getElementById('radio1').checked == true)
            location.href = '@Html.Raw(@Url.Action("RowColumnManipulation", new { @button= "Create", @SaveOption = "Xls" }))';
        else
            location.href = '@Html.Raw(@Url.Action("RowColumnManipulation", new { @button = "Create", @SaveOption = "Xlsx" }))';
        };
        document.getElementById('inputbtn').onclick = function () {
        if (document.getElementById('radio1').checked == true)
            location.href = '@Html.Raw(@Url.Action("RowColumnManipulation", new { @button = "Input Template", @SaveOption = "Xls" }))';
        else
            location.href = '@Html.Raw(@Url.Action("RowColumnManipulation", new { @button = "Input Template", @SaveOption = "Xlsx" }))';
        };
    </script>
}
