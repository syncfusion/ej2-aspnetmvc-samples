@using Syncfusion.EJ2

@section ActionDescription{
    This sample demonstrates how to create Box and Whisker chart using XlsIO.
    }

@section Meta{
    <meta name="description" content="This sample demonstrates how to create Excel file with box and whisker chart in ASP.NET MVC using Syncfusion XlsIO (Excel) library." />
}

@section ControlsSection{
<div class="control-section">
<div class="celldiv">
    <h5>
        Click the button to view an Excel spreadsheet generated by Essential XlsIO. Please
        note that Microsoft Excel Viewer or Microsoft Excel is required to view the resultant document.
    </h5>
    <br />
    <h5> Save As :</h5>
    <table>
        <tr>
            <td>
                @Html.EJS().RadioButton("radio1").Label("Embedded Chart").Name("select").Value("xls").Checked(false).Render()
            </td>
            <td>
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
            </td>
            <td>
                @Html.EJS().RadioButton("radio2").Label("Chart Sheet").Name("select").Value("xlsx").Checked(true).Render()
            </td>
        </tr>
    </table>
</div>
<br />
@Html.EJS().Button("createbtn").Content("Create Document").Render()
@Html.EJS().Button("inputbtn").Content("Input Template").Render()
</div>
<script>
    document.getElementById('inputbtn').onclick = function () {
        if (document.getElementById('radio1').checked)
            location.href = '@Html.Raw(@Url.Action("BoxAndWhisker", "XlsIO", new { @Saveoption = "embed", @button = "Input Template"  }))';
        else
            location.href = '@Html.Raw(@Url.Action("BoxAndWhisker", "XlsIO", new { @Saveoption = "sheet", @button = "Input Template"  }))';
    };
    document.getElementById('createbtn').onclick = function () {
        if (document.getElementById('radio1').checked)
            location.href = '@Html.Raw(@Url.Action("BoxAndWhisker", "XlsIO", new { @Saveoption = "embed", @button = "Create Document" }))';
        else
            location.href = '@Html.Raw(@Url.Action("BoxAndWhisker", "XlsIO", new { @Saveoption = "sheet", @button = "Create Document" }))';
    };
</script>
}
@section Description{
    A box and whisker chart shows distribution of data into quartiles, highlighting the mean and outliers. The boxes may have lines extending vertically called “whiskers”. These lines indicate variability outside the upper and lower quartiles, and any point outside those lines or whiskers is considered an outlier.<br />
    Essential XlsIO has APIs for creating and modifying Box and Whisker charts.
}