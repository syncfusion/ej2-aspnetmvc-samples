@using Syncfusion.EJ2


@section ActionDescription{
    This sample demonstrates how to create Treemap chart using XlsIO.
    }
@section Meta{
    <meta name="description" content="This sample demonstrates how to create Excel file with treemap in ASP.NET MVC using Syncfusion XlsIO (Excel) library." />
}
@section Description{

    A treemap chart provides a hierarchical view of your data and makes it easy to spot patterns, such as which items are a store's best sellers. The tree branches are represented by rectangles and each sub-branch is shown as a smaller rectangle. The treemap chart displays categories by color and proximity and can easily show lots of data which would be difficult with other chart types.
    <br /><br />Essential XlsIO has APIs for creating and modifying treemap charts.
}
@section ControlsSection{
    <div class="control-section">
        <h5>
            Click the button to view an Excel spreadsheet generated by Essential XlsIO. Please
            note that Microsoft Excel Viewer or Microsoft Excel is required to view the resultant document.
        </h5>
        <br />
        <h5>  Save As :</h5>
        <div class="celldiv">
            <table>
                <tr>
                    <td>
                        @Html.EJS().RadioButton("radio1").Label("Embedded Chart").Name("select").Value("xls").Checked(false).Render()
                    </td>
                    <td>
                        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                    </td>
                    <td>
                        @Html.EJS().RadioButton("radio2").Label("Chart Sheet").Name("select").Value("xlsx").Checked(true).Render()
                    </td>
                </tr>
            </table>
        </div>
        <br />
        <div class="celldiv">
            @Html.EJS().Button("createbtn").Content("Create Document").Render()
            @Html.EJS().Button("inputbtn").Content("Input Template").Render()
        </div>
    </div>
    <script>
        document.getElementById('createbtn').onclick = function () {
            if (document.getElementById('radio1').checked == true)
                location.href = '@Html.Raw(@Url.Action("Treemap", new { @button = "Create Document", @Saveoption = "embed" }))';
            else
                location.href = '@Html.Raw(@Url.Action("Treemap", new { @button = "Create Document", @Saveoption = "sheet" }))';
            };
        document.getElementById('inputbtn').onclick = function () {
            if (document.getElementById('radio1').checked == true)
                location.href = '@Html.Raw(@Url.Action("Treemap", new { @button = "Input Template", @Saveoption = "embed" }))';
            else
                location.href = '@Html.Raw(@Url.Action("Treemap", new { @button = "Input Template", @Saveoption = "sheet" }))';
            };
    </script>
}