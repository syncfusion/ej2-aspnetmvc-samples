@using Syncfusion.EJ2
@model EJ2MVCSampleBrowser.Controllers.ComboboxModel

@section ControlsSection{
    @using (Html.BeginForm())
    {
        @Html.ValidationSummary(true)
<div class="col-lg-12 control-section">
    <div id="wrapper">
        @Html.EJS().ComboBoxFor(model => model.val).DataSource(@Model.data).Placeholder("e.g. Basketball").Render()
        <div>
            @Html.ValidationMessageFor(model => model.val)
        </div>
        <div id="submitbutton">
            @Html.EJS().Button("btn").Content("Submit").Render()
        </div>
    </div>
</div>

    }

    @*custom code start*@
<style>
    #wrapper {
        max-width: 246px;
        margin: 30px auto;
        padding-top: 50px;
    }

    #submitbutton {
        margin-top: 20px;
        margin-left: 65px;
    }

    #control-content #wrapper .field-validation-error {
        color: red;
    }
</style>
    @*custom code end*@

}
@section ActionDescription{
<div id="action-description">
    <p>
        This example demonstrates the strongly-typed helper of <code>ComboBox</code>. Edit the ComboBox's value and click the 'Submit' button to pass the modified value to controller.
    </p>
</div>
}
@section Description{
<div id="description">
    <p>
        In this sample, the ComboBox is rendered with strongly-typed helper (FOR control) and bind its value from controller using model binding. The modified value is bind to controller action "ComboBoxFor" when submit a page.
    </p>
</div>
}
@section Meta{
                    <meta name="description" content="This example demonstrates the ComboBoxFor in ASP.NET MVC ComboBox control. Explore here for more details."/>
                }
@section Title{
                    <title>ASP.NET MVC ComboBox ComboBoxFor Example - Syncfusion Demos </title> 
                }
@section Header{
                    <h1 class='sb-sample-text'>Example of ComboBoxFor in ASP.NET MVC ComboBox Control</h1>
                }